# Server Configuration
# The port on which the Express server will run
PORT=5000

# The environment mode (development/production)
NODE_ENV=development

# Firebase Admin SDK Configuration
# For local development, set this to the path of your Firebase service account JSON file
# Download the service account key from Firebase Console > Project Settings > Service Accounts
# Example: GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
GOOGLE_APPLICATION_CREDENTIALS=

# Alternatively, you can set the Firebase credentials explicitly (optional, for environments without ADC)
# These values can be found in your Firebase service account JSON file
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# CORS Configuration
# The URL of the frontend application for CORS policy
# Use http://localhost:5173 for local development
FRONTEND_URL=http://localhost:5173

# AI Service Configuration (for future phases)
# Obtain API keys from the respective provider consoles:
# - OpenAI: https://platform.openai.com/api-keys
# - Google Gemini: https://makersuite.google.com/app/apikey
# - Anthropic Claude: https://console.anthropic.com/
OPENAI_API_KEY=
GEMINI_API_KEY=
AI_SERVICE_PROVIDER=openai

# Rate Limiting Configuration
# Time window in milliseconds for rate limiting (default: 900000 = 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum number of requests allowed per window per IP (default: 100)
RATE_LIMIT_MAX_REQUESTS=100

# Backend Logging
# Log level for backend application (debug, info, warn, error, none)
# Hierarchy: debug < info < warn < error < none
# Development: Use 'debug' or 'info' for detailed API logs
# Production: Use 'error' or 'none' to minimize logging and improve performance
# Default: error (if unset)
# Controls verbosity of logger in src/utils/logger.js
# Loaded via dotenv; see src/utils/logger.js for usage
LOG_LEVEL=error

# Backblaze B2 Cloud Storage
# Get your B2 Key ID and Application Key from your Backblaze account
# The bucket name should be the name of the bucket you created for this project
B2_KEY_ID=YOUR_B2_KEY_ID
B2_APPLICATION_KEY=YOUR_B2_APPLICATION_KEY
B2_BUCKET_NAME=YOUR_B2_BUCKET_NAME