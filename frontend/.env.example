# Firebase Configuration
# Get these values from your Firebase project settings in the Firebase Console:
# 1. Go to https://console.firebase.google.com/
# 2. Select your project
# 3. Click the gear icon (Settings) > Project settings
# 4. Scroll down to "Your apps" section and click "Add app" if you haven't already
# 5. Copy the config values from the SDK setup snippet (Web app)

VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id_here
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id_here
VITE_FIREBASE_APP_ID=your_firebase_app_id_here

# Optional: Google Analytics measurement ID
# Found in Firebase Console > Project settings > General > Your apps > Web app > Config
# Look for "measurementId" in the config object
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id_here

# API Configuration
# Backend API base URL
# Use http://localhost:5000 for local development
# Use your production backend URL for deployment
VITE_API_BASE_URL=http://localhost:5000

# AI Configuration (for future phases)
# Feature flag to enable/disable AI features
# Set to true to enable, false to disable
VITE_AI_ENABLED=false

# Frontend Logging
# Log level for frontend application (debug, info, warn, error, none)
# Hierarchy: debug < info < warn < error < none
# Development: Use 'debug' or 'info' for detailed output
# Production: Use 'error' or 'none' to minimize logging and improve performance
# Default: error (if unset)
# Controls verbosity of logger in src/utils/logger.js
# Reference: https://vitejs.dev/guide/env-and-mode.html#env-variables
VITE_LOG_LEVEL=error